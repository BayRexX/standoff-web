const CONFIG = {
    // Сетевое взаимодействие
    SERVER_URL: 'wss://fps-game-server-raki.onrender.com',
    
    // Физика и движение
    PC_MOVE_SPEED: 0.25,        // Быстрая скорость на ПК
    MOBILE_MOVE_SPEED: 0.15,    // Средняя скорость на мобильных
    ROTATION_SPEED: 0.002,      // Чувствительность мыши/поворота
    PLAYER_HEIGHT: 1.7,         // Высота игрока
    PLAYER_RADIUS: 0.4,         // Радиус коллизии
    
    // Стрельба
    SHOOT_COOLDOWN: 300,        // КД между выстрелами (мс)
    BULLET_SPEED: 2,            // Скорость пули
    BULLET_LIFETIME: 1000,      // Время жизни пули (мс)
    MAX_SHOOT_DISTANCE: 50,     // Максимальная дальность стрельбы
    
    // Здоровье и урон
    MAX_HEALTH: 100,            // Максимальное здоровье
    DAMAGE_PER_SHOT: 34,        // Урон за выстрел
    
    // Физика
    GRAVITY: 0.03,              // Сила гравитации
    JUMP_FORCE: 0.25,           // Сила прыжка
    FRICTION: 0.9,              // Трение при движении
    
    // Графика
    FOV: 75,                    // Поле зрения
    NEAR_PLANE: 0.1,            // Ближняя плоскость отсечения
    FAR_PLANE: 1000,            // Дальняя плоскость отсечения
    SHADOW_QUALITY: 2048,       // Качество теней
    
    // Игровой мир
    MAP_SIZE: 200,              // Размер карты
    WALL_HEIGHT: 6,             // Высота стен
    OBSTACLE_COUNT: 5,          // Количество препятствий
};
